# @zaimokuza/create-dsh-plugin-hub

离线生成企业市场与子来源项目的 CLI。运行 `npx @zaimokuza/create-dsh-plugin-hub --help`；生成后使用 `npm install`、`npm run build`、`npm pack`。

Offline CLI for enterprise markets and source plugins. Run `npx @zaimokuza/create-dsh-plugin-hub --help`, then install, build and pack the generated project.

说明与示例 / Guide: https://github.com/zaimokuza-yoshiteru/dsh-plugin-hub#readme
